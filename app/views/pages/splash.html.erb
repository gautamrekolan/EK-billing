<!DOCTYPE html>
<html>
<head>
  <title>Easy Keeper Billing</title>
  <%= stylesheet_link_tag    "global.css" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>

  <!-- Dependencies -->
  <script src="/javascripts/jquery.js" type="text/javascript"></script>

  <!-- Core files -->
  <script src="/javascripts/jquery.alerts.js" type="text/javascript"></script>
  <link href="/javascripts/jquery.alerts.css" rel="stylesheet" type="text/css" media="screen" />

  <script language="javascript">

    function alert(message) {
      return jAlert(message, 'Easy Keeper Equine');
    }

</script>
</head>
<body>
    <div id="page">
        <div id="header">
            <div id="logo">
                <h1><%= link_to 'Easy Keeper Billing', root_path %></h1>
            </div>
            <div id="top_nav">
                <ul style="padding: 0">
                  <li class="off"><a href="#" onclick="alert('Coming soon! Please check back.')">LOGIN</a>
                </ul>
            </div>
        </div>
        <div id="content">
            <div class="full_centered">
              <h1 class="title">Welcome!</h1>
              <p>You've found the future home of a comprehensive billing and invoice management application,
                 designed to help equine professionals (barn owners, barn managers, trainers and more)
                 maintain all of their records as easily as the <b>chubby pony</b> in your field maintains his weight.</p>
              <p>We're not up and running yet, but if you're interested and would like to be updated as we make progress,
                 feel free to leave your email address and we'll keep you posted. No spam, promise!</p>
                 <div id="signup">
                    <%= form_for(@potential, :url => :signup) do |f| %>
                        <% if @potential.errors.any? %>
                            <div id="signup_error_explanation">
                                <ul>
                                <% @potential.errors.each_with_index.each do |msg, i| %>
                                    <li><%= msg[1] %></li>
                                <% end %>
                                </ul>
                            </div>
                        <% end %>
                        <div id="signup_form">
                            <% if notice.blank? == false %>
                                <p id="notice"><%= notice %></p>
                            <% end %>
                            <% if @warning.blank? == false %>
                                <p id="warning"><%= @warning %></p>
                            <% end %>
                            <%= f.text_field :email, :value => "me@example.com", :class => "signup_text" %>
                            <%= f.submit "SUBMIT", :class => "signup_button" %>
                        </div>
                    <% end %>
                   </div>
            </div>
         </div>
        <div id="footer">
            <ul>
                <li><a href="#" onclick="alert('Coming soon! Please check back.')">ABOUT</a></li>
                <li><a href="#" onclick="alert('Coming soon! Please check back.')">CONTACT</a></li>
            </ul>
        </div>
    </div>
</body>
</html>